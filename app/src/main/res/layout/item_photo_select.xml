<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.view.View"/>
        <variable
            name="holder"
            type="com.us.traystorage.app.common.PhotoSelectActivity.ImageListAdapter.ListItemViewHolder" />
        <variable
            name="data"
            type="com.us.traystorage.app.common.PhotoSelectActivity.PhotoSelectModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:padding="2dp">

        <ImageView
            android:onClick="@{()->holder.onCameraClick()}"
            android:id="@+id/btn_camera"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="center"
            android:background="@color/Cf3f5f9"
            android:src="@drawable/icon_c_camera_30"/>

        <RelativeLayout
            android:onClick="@{()->holder.onClick()}"
            android:id="@+id/view_photo"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_focus_photo"
            app:isSelect="@{data.isSelected}"
            android:visibility="visible">

            <ImageView
                android:id="@+id/image_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="fitXY"
                android:src="@drawable/logo_symbol_60" />

            <com.us.traystorage.app.common.font.FontTextView
                style="@style/font_spoqahansansneo_regular"
                android:layout_width="26dp"
                android:layout_height="26dp"
                android:layout_alignParentEnd="true"
                android:background="@drawable/bg_btn_circle_photo"
                android:text="@{data.selectedIndex}"
                android:includeFontPadding="false"
                app:isSelect="@{data.isSelected}"
                android:visibility="@{data.isSingle?View.GONE:View.VISIBLE}"
                android:gravity="center"
                android:textColor="@color/white"
                android:textSize="15dp" />
        </RelativeLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>